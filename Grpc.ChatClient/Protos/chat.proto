syntax = "proto3";
 
option csharp_namespace = "Grpc.Chat";
 
package chat;
 
service Chat {
  rpc SendMessage (stream ChatMessage) returns (stream ChatMessage);
}
 
message ChatMessage {
  string message = 1;
}
